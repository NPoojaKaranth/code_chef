t = int(input())

for _ in range(t):

    n, k = map(int, input().split())

    arr = list(map(int, input().split()))

    count = 0

    for h in arr:
        if h > k:
            count += 1

    print(count)
  '''
  Red Light, Green Light

You are given N players standing between Gi-Hun and Ali.
Gi-Hun and Ali both have height K.

A player blocks Gi-Hun’s view only if their height is strictly greater than K.

Find the minimum number of players that must be removed so that Gi-Hun can see Ali.
  '''
